import React from 'react';
import './styles.css';

const SubTitle = ({ value }) => (
  <div className="subTitle-box">
    <h2 className="subTitle-text">{value}</h2>
  </div>
);

export default SubTitle;